# Do not include node_modules in the repo, these files are configured by the package.json dependencies
node_modules

# Do not include log files (most commonly npm error logs)
*.log

# Do not include changes to local config files
config/environment.js
